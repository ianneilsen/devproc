## Kernel - working with the linux kernel


#### check modules

```bash
lsmod
```

#### Add a kernel module

```bash
	/etc/modules-load.d

	modprobe name
	modprobe -r name

	insmod
	rmmod
```

#### Check modules

